extends layout

block content
  link(rel='stylesheet', href='/stylesheets/style.css')
  .w3-container
    if locals.user
      if user.breached === true
        +resourcesnormal
      if user.breached === false
        +resourcessecured
      else
        h1 idk what you're tryna do