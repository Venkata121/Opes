extends layout

block content
  link(rel='stylesheet', href='/stylesheets/style.css')
  .w3-card-4
    if locals.user
      pre
        code=user
      //if user.app_metadata.breached === true
        +resourcesnormal
      //if user.app_metadata.breached === false
        +resourcessecured
        h1 idk what you're tryna do