extends layout

block content
  .w3-card-4
    h1 Welcome, #{user.displayName}!
    img(src=user.picture)
    br
    each subject in subjects
      a.qblink(href="https://sarthakmohanty.me/apc/QB/" + subject) #{subject}
      br