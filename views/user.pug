extends layout

block content
  .w3-card-4
    h1 Welcome, #{user.displayName}!
    img(src=user.picture)
    each subject in subjects
      - var apcurl = "https://sarthakmohanty.me/apc/QB/${subject}"
      a(href=apcurl) subject