extends layout

block content
  .w3-card-4
    h1 Welcome, #{user.displayName}!
    img(src=user.picture)
    each subject in subjects {
      if user.subject === subject {
        p subject
      }
      else {
        p
      }
    }